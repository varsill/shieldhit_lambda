from environments.remote_mapper import RemoteMapper

class Mapper(RemoteMapper):
    """
    Module used for samples generation with AWS Lambda environment. Implements 'environment.mapper.Mapper' interface.
    """
    pass